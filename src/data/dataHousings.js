import housings from "./logements.json";

export const getAll = () => {
  return housings;
};
/*export const getOne = () => {
  return housings.find((housing) => housing.id === id);
};*/

//export const dataHousings = { getAll, getOne };
